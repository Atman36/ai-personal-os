# Project-scoped Codex configuration for Personal OS.
# Keep this file conservative. Agents inherit the parent session model unless a custom agent overrides it.

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800

# Optional MCP examples. Enable only after you trust the boundary and have tokens configured.
# [mcp_servers.context7]
# command = "npx"
# args = ["-y", "@upstash/context7-mcp"]

# [mcp_servers.openaiDeveloperDocs]
# url = "https://developers.openai.com/mcp"

# [mcp_servers.github]
# command = "npx"
# args = ["-y", "@modelcontextprotocol/server-github"]
# env_vars = ["GITHUB_PERSONAL_ACCESS_TOKEN"]
