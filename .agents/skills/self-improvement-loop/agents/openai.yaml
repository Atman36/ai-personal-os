interface:
  display_name: "Self-Improvement Loop"
  short_description: "Capture, synthesize, promote, and consume Personal OS reflection signals"
  default_prompt: "Use $self-improvement-loop to process Personal OS reflections, promote only validated improvements, and delete consumed active reflection records."
